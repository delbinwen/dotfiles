# ~/.zshrc.local
# 機器專屬設定（不同電腦的差異部分）
# 此檔案由 .zshrc 載入，適合放：
#   - 特定電腦的路徑
#   - 私密資訊（不要提交到 git）
#   - 只在這台機器需要的工具設定
#
# 私密資訊建議用 Keychain 存取，例如：
#   export MY_TOKEN=$(security find-generic-password -a "$USER" -s "MY_TOKEN" -w)
